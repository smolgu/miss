package vk

import "fmt"

// GetPhotos get info about user. Returns 5 top (with more likes and
// conain only one face) user photos
func GetPhotos(token string, vkID int) ([]string, error) {
	return nil, fmt.Errorf("vk.GetPhotos not implemented")
}
